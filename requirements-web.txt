# Zusätzliche Abhängigkeiten für das Web-Frontend (webapp.py)
Flask>=3.0
gunicorn>=21.2

# Multi-User-Fundament (Phase 1): Postgres-Datenmodell, Auth, Secrets-Verschlüsselung.
Flask-SQLAlchemy>=3.1
Flask-Login>=0.6
psycopg2-binary>=2.9
alembic>=1.13
cryptography>=42

# Multi-User Phase 3: Job-Queue (statt threading.Thread), Object Storage für
# PDFs/APKGs (statt lokalem Disk), Rate-Limiting.
rq>=1.16
redis>=5.0
boto3>=1.34
Flask-Limiter>=3.5

# SRS-Vokabeltrainer (Fundament, siehe README-Roadmap): FSRS-Scheduling-
# Algorithmus statt einer eigenen SM-2-Nachimplementierung.
fsrs>=6.0

# API-Dokumentation (Swagger/OpenAPI-UI unter /api/docs, siehe webapp.py).
flasgger>=0.9.7

# Error-Tracking (nur aktiv, wenn SENTRY_DSN gesetzt ist, siehe webapp.py).
sentry-sdk[flask]>=2.0
